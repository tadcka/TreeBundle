{% for type, message in messages %}
    <div class="alert alert-{{ type }}" role="alert">{{ message }}</div>
{% endfor %}

{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_rest(form) }}
{{ form_end(form) }}